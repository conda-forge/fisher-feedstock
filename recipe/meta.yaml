{% set name = "fisher" %}
{% set version = "0.1.15" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: https://github.com/brentp/fishers_exact_test/archive/refs/tags/v{{ version }}.tar.gz
  sha256: be3c7151f59a13e540b9ea88ca77548314944efae7cc559e25afa954bd4e32a5

build:
  number: 3
  script:
    - "{{ PYTHON }} -m pip install . --no-deps --ignore-installed"

requirements:
  build:
    - python                                 # [build_platform != target_platform]
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
    - cython                                 # [build_platform != target_platform]
    - numpy                                  # [build_platform != target_platform]
    - {{ compiler('c') }}
    - {{ stdlib('c') }}

  host:
    - python
    - cython
    - numpy
    - pip
    - versioneer
    - setuptools
  run:
    - python
    - numpy

test:
  imports:
    - fisher

about:
  home: http://github.com/brentp/fishers_exact_test
  license_file: LICENSE
  license: BSD-3-Clause
  summary: Simple, fast implementation of Fisher's exact test

extra:
  recipe-maintainers:
    - npavlovikj
    - maxyme
